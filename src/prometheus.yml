global:
  scrape_interval: 5s # Scrape metrics every 5 seconds

scrape_configs:
  - job_name: "tsoa-express-api"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["host.docker.internal:4000"] # Points to local Express server port
